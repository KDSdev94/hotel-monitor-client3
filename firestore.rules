rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Izinkan akses baca/tulis untuk semua selama tahap pengembangan awal
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
